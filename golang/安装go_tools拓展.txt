现在的 Go 插件需要新版的 gopls 等工具，而这些工具 最低要求 Go 1.21+。
go1.23.3.linux-amd64.tar.gz
sudo rm -rf /usr/local/go
tar -xvf 解压go压缩包
将go目录路径移动到/usr/local下
sudo mv ~/Downloads/go /usr/local/ （根据自己解压的go路径移动）

echo 'export PATH=$PATH:/usr/local/go/bin' >> ~/.bashrc
source ~/.bashrc

go version 验证安装go版本
which go 查看go安装路径

// 自动安装
ctrl + shift + p: Go: Locate Configured Go Tools

// 手动安装
    # 1️⃣ 设置国内代理（清华镜像）
    go env -w GOPROXY=https://goproxy.cn,direct

    # 2️⃣ 确保 GOPATH 存在（一般是 ~/go）
    mkdir -p $(go env GOPATH)/bin

    # 3️⃣ 安装 VS Code 常用 Go 工具
    /usr/local/go/bin/go install golang.org/x/tools/gopls@latest
    /usr/local/go/bin/go install github.com/go-delve/delve/cmd/dlv@latest
    /usr/local/go/bin/go install golang.org/x/tools/cmd/goimports@latest
    /usr/local/go/bin/go install honnef.co/go/tools/cmd/staticcheck@latest
    /usr/local/go/bin/go install github.com/uudashr/gopkgs/v2/cmd/gopkgs@latest
    /usr/local/go/bin/go install github.com/fatih/gomodifytags@latest
    /usr/local/go/bin/go install github.com/josharian/impl@latest
    /usr/local/go/bin/go install github.com/cweill/gotests/gotests@latest

    # 4️⃣ 把 Go 工具路径加入环境变量
    echo 'export PATH=$PATH:$(go env GOPATH)/bin' >> ~/.bashrc
    source ~/.bashrc

    # 5️⃣ 验证安装结果
    echo "✅ Go 版本：$(go version)"
    echo "✅ gopls 路径：$(which gopls)"
    echo "✅ GOPROXY：$(go env GOPROXY)"


// 手动指定vscode的go路径
打开设置 JSON 文件（Ctrl + Shift + P → 输入 “Preferences: Open Settings (JSON)”）

"go.goroot": "/usr/local/go",
"go.toolsManagement.go": "/usr/local/go/bin/go"
"go.alternateTools": {
    "gopls": "/home/mentha/go/bin/gopls"
}
